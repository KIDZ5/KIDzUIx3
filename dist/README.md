# KIDzUIx3 Distribution Builds

Prebuilt **KIDzUIx3 v1.0.0** release files.

- `dist.luau` — standard compiled build
- `dist-unminified.luau` — readable unminified build
- `dist-typed.luau` — typed build

These are the files intended to be attached to the GitHub `v1.0.0` release.
